// Importing the method for creating contexts
import { createContext } from "react";

export const CandidateContext = createContext();
export const CandidateContextProvider = CandidateContext.Provider;
